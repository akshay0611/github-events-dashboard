/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}", // This tells Tailwind to look for Tailwind classes in all JS, JSX, TS, and TSX files in the 'src' directory.
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
